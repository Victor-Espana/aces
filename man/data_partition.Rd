% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tune_model.R
\name{data_partition}
\alias{data_partition}
\title{Create Data Partitions for ACES Tuning}
\usage{
data_partition(
  data,
  x,
  y,
  validation = "kfold",
  resampling_args = list(),
  delta = 1.05,
  seed = NULL
)
}
\arguments{
\item{data}{A data frame or matrix containing the model variables.}

\item{x}{Column indexes of input variables in \code{data}.}

\item{y}{Column indexes of output variables in \code{data}.}

\item{validation}{Resampling method: \code{"kfold"}, \code{"repeated_kfold"},
\code{"holdout"}, \code{"bootstrap"}, \code{"loo"}, or \code{"group_kfold"}.}

\item{resampling_args}{A list of method-specific resampling parameters.
Valid elements depend on \code{validation}:
\itemize{
\item \code{kfold}: \code{folds} (default 5), \code{strata} (default \code{NULL}).
\item \code{repeated_kfold}: \code{folds} (default 5), \code{repeats} (default 1),
\code{strata} (default \code{NULL}).
\item \code{holdout}: \code{repeats} (default 1), \code{train_prop} (default 0.8),
\code{strata} (default \code{NULL}).
\item \code{bootstrap}: \code{repeats} (default 1).
\item \code{loo}: no arguments needed.
\item \code{group_kfold}: \code{folds} (default 5), \code{groups} (required).
}
Unrecognized elements trigger an error.}

\item{delta}{Maximum output-oriented DEA score included in validation. This
limits the influence of observations that lie far beyond the training
technology; it must be at least 1.}

\item{seed}{Optional integer seed used to make the resampling splits
reproducible.}
}
\value{
An object of class \code{aces_partition} containing the data, input and
output indexes, resampling indexes, \code{delta}, and seed.
}
\description{
Creates reusable resampling splits for \code{\link[=tune_model_aces]{tune_model_aces()}} and
\code{\link[=tune_model_rf_aces]{tune_model_rf_aces()}}. The same partition can be used to compare models on
identical samples. The partition stores all information required for tuning,
so candidate configurations are evaluated under the same validation design.
}
\examples{
partition <- data_partition(
  data = cobb_douglas_XnY1(N = 30, nX = 3),
  x = 1:3,
  y = 4,
  resampling_args = list(folds = 3),
  delta = 1.5,
  seed = 42
)

}
