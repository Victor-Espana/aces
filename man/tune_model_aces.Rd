% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tune_model.R
\name{tune_model_aces}
\alias{tune_model_aces}
\alias{tune_model_rf_aces}
\title{Tune an ACES Model}
\usage{
tune_model_aces(
  partition,
  scale_data = TRUE,
  quick_aces = FALSE,
  mul_BF = list(max_degree = 1, inter_cost = 0.05),
  metric = "mse",
  shape = list(mono = TRUE, conc = TRUE),
  max_terms = 50,
  err_red = 0.01,
  kn_grid = -1,
  minspan = -1,
  endspan = -1,
  kn_penalty = 2,
  validation_metric = "mse",
  returns = "variable",
  method = "aces",
  verbose = TRUE
)

tune_model_rf_aces(
  partition,
  scale_data = TRUE,
  quick_aces = FALSE,
  mul_BF = list(max_degree = 1, inter_cost = 0.05),
  metric = "mse",
  shape = list(mono = TRUE, conc = TRUE),
  learners = 100,
  bag_size = nrow(partition$data),
  max_feats = length(partition$x)/3,
  early_stopping = list(ma_window = 10, tolerance = 5),
  max_terms = 50,
  err_red = 0.01,
  kn_grid = -1,
  minspan = -1,
  endspan = -1,
  validation_metric = "mse",
  returns = "variable",
  method = "rf_aces",
  verbose = TRUE
)
}
\arguments{
\item{partition}{An object created by \code{\link[=data_partition]{data_partition()}}.}

\item{scale_data}{Candidate logical values. If \code{TRUE}, divide each input and
output by its mean before fitting; this can improve solver convergence.}

\item{quick_aces}{Candidate logical values. If \code{TRUE}, reduce the candidate
basis functions considered during the forward step.}

\item{mul_BF}{A list with candidate values for \code{max_degree}, the maximum
interaction degree, and \code{inter_cost}, the extra improvement required to add
a higher-degree basis function.}

\item{metric}{Candidate lack-of-fit measures used while fitting ACES. See
\code{\link[=aces]{aces()}} for the available values.}

\item{shape}{A list with candidate logical values for \code{mono}, which enforces
non-decreasing monotonicity, and \code{conc}, which enforces concavity. When both
are \code{TRUE}, the output-specific fits are production functions; otherwise,
they are intermediate predictors of maximum attainable output used to
construct the final technology.}

\item{max_terms, err_red, kn_grid, minspan, endspan, kn_penalty}{Additional \code{\link[=aces]{aces()}}
parameters. Each may contain one or several values. A custom list supplied
to \code{kn_grid} is treated as one value; use a list of custom grids to compare
several of them.}

\item{validation_metric}{One or more validation error measures. All supplied
measures are computed; the first selects the best configuration. Options:
\code{"mae"}, \code{"mape"}, \code{"mse"}, \code{"msle"}, \code{"rmse"}, \code{"rmsle"},
\code{"nrmse_mean"}, \code{"nrmse_range"}.}

\item{returns}{Returns-to-scale assumption used by the frontier-aware
validation weights: \code{"variable"} or \code{"constant"}.}

\item{method}{RF-ACES model used to generate validation predictions. Available
methods are \code{"rf_aces"}, \code{"rf_aces_cubic"}, and \code{"rf_aces_quintic"}.}

\item{verbose}{If \code{TRUE}, print the current configuration and split while
tuning.}

\item{learners}{Candidate positive integers giving the maximum number of ACES
learners in the forest.}

\item{bag_size}{Candidate positive integers giving the number of observations
sampled with replacement for each learner.}

\item{max_feats}{Candidate values for the number of inputs randomly considered
by each learner.}

\item{early_stopping}{A list with candidate values for \code{ma_window}, the OOB
moving-average window, and \code{tolerance}, the allowed consecutive iterations
without improvement.}
}
\value{
An object of class \code{aces_tune} with the best parameters, best
scores for every computed metric, results for every configuration, and
the final model fitted to all data.
}
\description{
Builds a parameter grid and selects the ACES configuration with the smallest
frontier-aware validation error. Each model argument may contain one or more
candidate values. After comparing all configurations on the stored splits, the
best configuration is fitted once more using the complete data set.

Builds a parameter grid and selects the RF-ACES configuration with the
smallest frontier-aware validation error. After comparing all configurations
on the stored splits, the best configuration is fitted once more using the
complete data set.
}
\details{
Arguments have the same form as in \code{\link[=aces]{aces()}}. To tune elements of a list
argument, place vectors inside the list. For example,
\code{mul_BF = list(max_degree = c(1, 2), inter_cost = c(0.01, 0.05))}.

The model arguments have the same form as in \code{\link[=rf_aces]{rf_aces()}}. Vectors inside
\code{mul_BF}, \code{shape}, and \code{early_stopping} are expanded internally.
}
\examples{
\dontrun{
set.seed(42)
production_data <- cobb_douglas_XnY1(N = 100, nX = 6)
partition <- data_partition(
  production_data,
  x = 1:6,
  y = 7,
  validation = "holdout",
  resampling_args = list(repeats = 2),
  delta = 1.5,
  seed = 42
)
tuned <- tune_model_aces(
  partition,
  quick_aces = TRUE,
  mul_BF = list(
    max_degree = c(1, 2),
    inter_cost = c(0.01, 0.05)
  ),
  err_red = c(0.01, 0.05),
  validation_metric = "rmse"
)
}

\dontrun{
set.seed(42)
production_data <- cobb_douglas_XnY1(N = 100, nX = 6)
partition <- data_partition(
  production_data,
  x = 1:6,
  y = 7,
  validation = "holdout",
  resampling_args = list(repeats = 2),
  delta = 1.5,
  seed = 42
)
tuned <- tune_model_rf_aces(
  partition,
  quick_aces = TRUE,
  mul_BF = list(
    max_degree = c(1, 2),
    inter_cost = 0.05
  ),
  learners = 3,
  bag_size = c(60, 80),
  max_feats = c(2, 4),
  validation_metric = "rmse"
)
}

}
